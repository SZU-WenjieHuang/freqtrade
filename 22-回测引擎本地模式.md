

你这次改动的核心目标是：**让 Freqtrade 的回测流程在不能访问交易所网络接口时，也能用本地已有数据跑起来。**

也就是说，原来 Freqtrade 回测虽然 K 线数据可以是本地的，但它初始化 `exchange` 时，仍然会依赖交易所接口加载一些市场元信息，例如：

- **markets / exchangeInfo**
- **交易对精度**
- **最小下单数量**
- **手续费**
- **期货杠杆档位 leverage tiers**
- **合约大小 contract size**
- **强平价计算所需参数**

你做的改动，就是在回测初始化阶段加了一个 **offline exchange metadata fallback**。

---

## 原来的问题在哪里

你提到的这段：

```python
exchange = ExchangeResolver.load_exchange(self.config, load_leverage_tiers=True)
```

或者现在附近这段：

```python
exchange = ExchangeResolver.load_exchange(
    self.config,
    validate=not offline_backtest_enabled,
    load_leverage_tiers=not offline_backtest_enabled,
)
```

这里是 `Backtesting` 初始化 `exchange` 的地方。

在正常模式下，`ExchangeResolver.load_exchange(...)` 会创建交易所对象，比如 Binance Futures，然后会做一些初始化：

- 校验交易所可用性
- 加载 markets
- 加载 leverage tiers
- 设置精度、费用、限额等交易规则

这些信息通常来自 Binance 的接口，比如 futures 的 `exchangeInfo`。

所以即使你已经有本地 K 线数据，Freqtrade 仍然可能联网，因为它需要知道：

- `BTC/USDT:USDT` 是否存在
- 数量精度是多少
- 价格精度是多少
- 最小下单名义价值是多少
- futures 杠杆档位是多少
- 合约大小是多少

没有这些信息，回测就无法正确模拟下单。

---

## 你现在加的离线模式

你加了一个环境变量开关：

```python
OFFLINE_BACKTEST_ENV = "FREQTRADE_OFFLINE_BACKTEST"
```

并提供判断函数：

```python
def is_offline_backtest_enabled() -> bool:
    return os.environ.get(OFFLINE_BACKTEST_ENV, "").lower() in {"1", "true", "yes", "on"}
```

也就是说，只要运行时设置：

```bash
FREQTRADE_OFFLINE_BACKTEST=1
```

回测就会进入离线模式。

---

## 关键改动一：初始化 exchange 时禁止联网校验和加载 leverage tiers

你现在的逻辑是：

```python
offline_backtest_enabled = is_offline_backtest_enabled()
if not exchange:
    exchange = ExchangeResolver.load_exchange(
        self.config,
        validate=not offline_backtest_enabled,
        load_leverage_tiers=not offline_backtest_enabled,
    )
    if offline_backtest_enabled:
        apply_offline_markets_cache(exchange, self.config)
self.exchange = exchange
```

这个改动非常关键。

### 正常在线模式

如果没有设置 `FREQTRADE_OFFLINE_BACKTEST=1`：

```python
validate=True
load_leverage_tiers=True
```

行为基本保持原样：

- 创建真实 exchange 实例
- 校验交易所
- 加载 leverage tiers
- 正常依赖交易所接口

### 离线模式

如果设置了：

```bash
FREQTRADE_OFFLINE_BACKTEST=1
```

则变成：

```python
validate=False
load_leverage_tiers=False
```

含义是：

- **不做交易所在线校验**
- **不主动从交易所加载期货杠杆档位**
- 先创建一个 exchange 对象壳子
- 然后用本地 cache 手动注入 markets / fees / leverage tiers

这就是“没有 exchange 网络数据时”的核心兜底。

严格说，不是完全没有 `exchange` 对象，而是：

> **仍然创建 exchange 对象，但不让它联网加载市场信息，而是把本地缓存的市场元信息塞进去。**

---

## 关键改动二：从本地 `markets_cache_minimal.json` 加载市场信息

你增加了：

```python
OFFLINE_MARKETS_CACHE_NAME = "markets_cache_minimal.json"
```

以及：

```python
def load_offline_markets_cache(config: Config) -> dict[str, Any]:
    user_data_dir = Path(config["user_data_dir"])
    cache_path = user_data_dir / OFFLINE_MARKETS_CACHE_NAME
```

它会去读：

```text
user_data/markets_cache_minimal.json
```

这个文件现在承担了原来 Binance `exchangeInfo` 的一部分职责。

里面至少需要包含：

- `markets`
- `fees`
- `precision_mode`
- 可选的 `currencies`
- 可选的 `leverage_tiers`

如果文件不存在，会直接报错：

```python
Offline backtesting is enabled, but market cache was not found
```

如果没有 `markets`，也会报错。

这个设计挺好，因为离线模式不能默默降级，否则后面会出现更难排查的下单失败。

---

## 关键改动三：把本地 markets 注入到 exchange 对象里

核心函数是：

```python
def apply_offline_markets_cache(exchange: Exchange, config: Config) -> None:
```

里面做了几件事。

### 1. 注入 markets

```python
exchange._markets = markets
```

这相当于告诉 Freqtrade：

> 不要去 Binance 查交易对信息了，这些就是本次回测可用的 market metadata。

比如 `BTC/USDT:USDT` 的：

- symbol
- id
- base / quote / settle
- precision
- limits
- contract size
- linear / inverse
- futures/swap 类型

都从本地 cache 来。

---

### 2. 注入手续费

```python
exchange._trading_fees = cache_data.get("fees") or {}
```

这样后续：

```python
self.exchange.get_fee(...)
```

可以从本地数据里拿到 fee。

否则回测过程中 `set_fee()` 这里会出问题：

```python
fees = [
    self.exchange.get_fee(
        symbol=self.pairlists.whitelist[0],
        taker_or_maker=mt,
    )
    for mt in ("taker", "maker")
]
```

---

### 3. 注入 futures leverage tiers

```python
exchange._leverage_tiers = cache_data.get("leverage_tiers") or create_offline_leverage_tiers(markets)
```

这个是 futures 回测非常关键的一步。

因为你当前配置是：

```json
"trading_mode": "futures"
```

Freqtrade 在 futures 模式下需要 leverage tiers 来计算：

- 最大杠杆
- 维持保证金率
- 强平价
- 风险限制

如果在线模式，它通常会从交易所加载。

离线模式下，你让它：

1. 优先用 `markets_cache_minimal.json` 里的 `leverage_tiers`
2. 如果没有，就用 `create_offline_leverage_tiers(markets)` 构造一个最小可用版本

---

## 关键改动四：构造离线 futures 杠杆档位

你加了：

```python
def create_offline_leverage_tiers(markets: dict[str, Any]) -> dict[str, list[dict[str, Any]]]:
```

它为每个 pair 构造类似这样的结构：

```python
{
    "minNotional": 0,
    "maxNotional": None,
    "maintenanceMarginRate": 0.004,
    "maxLeverage": max_leverage,
    "maintAmt": 0.0,
}
```

这就是一个最小可用的 futures 风控档位。

其中：

- **`minNotional`**：最低名义金额起点
- **`maxNotional`**：最高名义金额，不限制
- **`maintenanceMarginRate`**：维持保证金率，这里给了 `0.004`
- **`maxLeverage`**：从 market limits 里拿，没有就默认 `1`
- **`maintAmt`**：固定维持保证金金额，后来改成了 `0.0`

这里 `maintAmt` 从 `None` 改成 `0.0` 是必要的，因为 Binance futures 的强平价计算要求这个参数必须是数值。

之前报错就是：

```text
Parameter maintenance_amt is required by Binance.liquidation_price for futures
```

---

## 关键改动五：把本地 markets 设置到 ccxt 实例里

你还做了这段：

```python
for ccxt_api in (exchange._api, exchange._api_async):
    if precision_mode is not None:
        ccxt_api.precisionMode = precision_mode
    ccxt_api.set_markets(markets, currencies)
```

这一步非常重要。

Freqtrade 的 exchange 对象内部有 ccxt 实例：

- `exchange._api`
- `exchange._api_async`

很多精度转换、market 查询、symbol 标准化逻辑，最终还是走 ccxt 的 market 结构。

所以只改：

```python
exchange._markets = markets
```

还不够。

你还需要告诉 ccxt：

> 这些 markets 是已加载状态，请不要再去交易所 fetch markets。

所以调用：

```python
ccxt_api.set_markets(markets, currencies)
```

这样 ccxt 内部也有了：

- `markets`
- `markets_by_id`
- `symbols`
- `currencies`

---

## 你修过的一个关键坑：`precision_mode`

之前 0 交易的根因就是这里。

你的 `markets_cache_minimal.json` 里写的是类似：

```json
"precision": {
  "amount": 0.001,
  "price": 0.1
}
```

这属于 **tick size** 写法。

但之前：

```json
"precision_mode": 2
```

代表的是 `DECIMAL_PLACES` 模式。

这两个不匹配。

结果是本来要下：

```text
0.0034 BTC
```

但 Freqtrade/ccxt 按错误精度模式处理后变成：

```text
0.0 BTC
```

所以 `_enter_trade()` 直接不下单。

你后来把它改成：

```json
"precision_mode": 4
```

也就是 `TICK_SIZE` 模式。

修复后：

```text
amount_p = 0.003409...
amount = 0.003
```

于是回测终于能产生交易。

---

## 总结一句话

你这次对 `backtesting.py` 的核心改动是：

> **给 Freqtrade 回测加了一个离线 exchange metadata 注入机制。在线模式不变；离线模式下仍然创建 exchange 对象，但跳过交易所校验和 leverage tiers 联网加载，然后从 `user_data/markets_cache_minimal.json` 把 markets、fees、precision、leverage tiers 注入到 exchange 和 ccxt 实例里，让本地 K 线数据可以在没有 Binance API 的情况下完成 futures 回测。**

---

## 现在这套机制的运行链路

离线回测时整体链路是：

```text
FREQTRADE_OFFLINE_BACKTEST=1
        ↓
Backtesting.__init__()
        ↓
is_offline_backtest_enabled() == True
        ↓
ExchangeResolver.load_exchange(
    validate=False,
    load_leverage_tiers=False
)
        ↓
创建 exchange 对象，但不强依赖在线 market/leverage 数据
        ↓
apply_offline_markets_cache(exchange, config)
        ↓
读取 user_data/markets_cache_minimal.json
        ↓
注入 exchange._markets
注入 exchange._trading_fees
注入 exchange._leverage_tiers
设置 ccxt precisionMode
ccxt_api.set_markets(...)
        ↓
DataProvider / PairList / Wallet / Backtesting 正常使用 exchange
        ↓
用本地 OHLCV 数据完成回测
```

---

## 对 `exchange = ExchangeResolver.load_exchange(...)` 这行的理解

你可以这样理解：

原来它是：

> **创建 exchange，并让 exchange 自己去交易所把必要信息加载完整。**

现在离线模式下它变成：

> **只创建 exchange 的运行壳子，不让它联网加载关键市场信息；随后我们手动把本地缓存的市场信息补进去。**

所以它不是完全绕过 exchange，而是绕过了 **exchange 的在线初始化数据源**。  
Freqtrade 后续很多地方仍然依赖 `exchange` 对象，例如：

- 精度处理
- 下单金额校验
- fee 获取
- contract size 获取
- liquidation price 计算
- pairlist 校验
- futures 模式逻辑

因此必须保留 exchange，只是把数据源换成本地。